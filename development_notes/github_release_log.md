# GitHub release log

The target is the public repository `xiaowu20/DOMformulaR`. GitHub CLI 2.96.0 was installed on 2026-07-24 and authenticated through the operating-system keyring as `xiaowu20`, with `repo` and `workflow` scopes. The target repository did not exist before publication. No tag, release, DOI, or pkgdown deployment has yet been created.

Package owner, author, email, public visibility, badges, URLs, citation metadata, and pkgdown URL were updated from the user's explicit values. Local Git was initialized on `main` with repository-local author settings.

After those items are supplied, the release sequence is local Git initialization, review of the exact staged file list, secret and private-data scan, initial signed-off commit where available, remote repository creation, branch protection-compatible push, GitHub Actions verification, pkgdown deployment, version `v0.1.0` tag, GitHub release, and optional Zenodo archival. A Zenodo DOI must not be inserted before it exists.
