# GitHub release log

The public repository `xiaowu20/DOMformulaR` was created on 2026-07-24. GitHub CLI 2.96.0 was installed and authenticated through the operating-system keyring as `xiaowu20`, with `repo` and `workflow` scopes. The initial root commit was `f6b918a`.

Package owner, author, email, public visibility, badges, URLs, citation metadata, and pkgdown URL were updated from the user's explicit values. Pull request 1 corrected the pkgdown index and CI coverage reporting and was squash-merged as `33af777`.

The lint, coverage, pkgdown, Windows R release, macOS R release, Ubuntu R release, and Ubuntu R oldrel checks passed. Overall CI coverage was 90.29%. GitHub Pages was enabled from `gh-pages` and reported `built` with HTTPS enforced at `https://xiaowu20.github.io/DOMformulaR/`. The R devel matrix remained in dependency installation at the release decision point and had not reported a package failure.

The version `v0.1.0` tag and GitHub release are created only after this audit update is merged. No Zenodo DOI has been minted, and none is inserted into package metadata.
